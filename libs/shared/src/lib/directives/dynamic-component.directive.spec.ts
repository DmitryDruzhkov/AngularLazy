import { DirectivesDynamicComponentDirective } from './directives/dynamic-component.directive';

describe('DirectivesDynamicComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesDynamicComponentDirective();
    expect(directive).toBeTruthy();
  });
});
